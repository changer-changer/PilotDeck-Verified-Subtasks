# 验证记录 · 2026-09-11

## 结论

实现了契约预检、JSON/宿主产物检查、独立模型复核、原子任务内有界修复、明确拒绝、累计用量和原生配置界面。使用比赛发放的 GLM-5.3，从原生网关到主任务、四个子任务、模型评审和实际文件修复的完整链路已跑通。可以证明受控场景中的语义错误拦截与局部修复；目前不能证明真实任务的普遍成功率提升。

## 比赛模型：原生双层验收实跑

2026-09-11，使用比赛端点的 `glm-5.3`。所有模型回答、文件读写、评审与修复均真实执行；明确预置一份业务错误简报，三份报表正确。简报首次内容为“供应稳定、无风险、无需跟进”，而来源要求说明延期 7 天、影响 18 单，并明确采购负责人、下周一12:00与确认备选供应商交期。

| 核对项 | 实际结果 |
|---|---|
| 原生链路 | `createLocalGateway → submitTurn → agent → SubAgentSession → 两层验收 → 主任务总结` 正常完成 |
| 四项最终交付 | 4/4 验收通过，主任务没有额外创建子任务 |
| 错误简报首次 | `checksPassed: true`；模型独立读取简报与源文件后 `rejected` |
| 局部修复 | 1 次；同一个子任务再次提交后 `accepted` |
| 成功兄弟任务 | 销售、库存、退款三份文件的 SHA-256 和修改时间均保持不变 |
| 整轮墙钟时间 | 160,474 ms，含主任务、四个子任务、评审及修复；单次运行，不是平均值 |
| 独立评审开销 | 13 次模型调用，接口报告总 token 58,042（含缓存相关用量） |
| 四个子任务含评审 | 合计 25 次模型调用，接口报告总 token 151,619；其中已包含上述评审用量，不能再次相加 |
| 主任务用量 | 单独报告总 token 31,542；不据此估算费用，模型端点未提供计费结果 |

成功证据：[摘要与断言](evidence/native-model-review/03-typed-review-success/summary.json)、[逐次报告](evidence/native-model-review/03-typed-review-success/reports.json)、[事件](evidence/native-model-review/03-typed-review-success/native-results.json)、[实际产物](evidence/native-model-review/03-typed-review-success/workspace/briefing.json)。公开副本保留问题、工具结果、判定、用量和失败；脱敏路径与端点，并移除模型思考流，详见证据说明。

这不是自然错误率测试：首次错误由演示任务明确要求提交。没有在相同模型、相同预算下做双层方案与整批重跑的充分对照，因此不能把后文第一层的 40% 请求节省套到双层方案。

### 调试失败也保留

| 运行 | 结果与后续修正 |
|---|---|
| `01-freeform-verdict-failed` | 285.126 秒；2/4 通过，部分评审输出在 JSON 前添加文字，系统按无效判定拒绝。改用专用 `structured_output` 交付评审结果。演示同时固定选定模型，关闭不必要的自动路由分类。 |
| `02-stale-claim-boundary-failed` | 360.597 秒；出现 4 次通过，但没有框架局部修复事件，主任务最终超时，不计成功。生产子任务提交后仍继续修改文件，形成旧声明与新文件错位。修正为带验收任务在结构化提交时结束该次执行，并以宿主 `matchClaimOnly` 检查声明与实际文件一致。 |
| `03-typed-review-success` | 160.474 秒；真实模型拒绝 → 同一子任务修复 → 再次通过，全部独立断言成立。 |

三次完整运行公开在 [实验索引](evidence/native-model-review/index.json)，不是从自然任务抽取的三个独立样本。另有两次单独评审诊断：自由文本判定格式不合格；改成结构化工具后正确拒绝预置简报。它们不计入完整运行的成功率。

## 第一层历史对照：真实文件的故障注入

三项独立任务分别生成销售、库存和退款 JSON 文件。首次销售总额注入 196（正确值 186），其他任务正确。使用真实 PilotDeck AgentLoop、读写工具、权限层、宿主文件检查器；仅模型响应为预设。每组总请求上限 30。

| 策略 | 实际产物通过 | 模型请求 | 写文件次数 |
|---|---:|---:|---:|
| 仅验收，不修复 | 2/3 | 6 | 3 |
| 整批重跑 | 3/3 | 15 | 6 |
| 原子任务内局部修复 | 3/3 | 9 | 4 |

该情形下局部修复比整批重跑少 40% 请求，两个成功兄弟任务均只写一次。整批重跑在第二轮重新读取旧文件后写入，遵循 PilotDeck 文件新鲜度要求。没有模拟 token 或将模型调用数冒充真实 token 节省；不外推真实任务平均加速。

原始记录：交付包 `evidence/deterministic-final/results.json`，每个策略目录的 `trace.json` 包含完整事件、报告与文件。可交互回放：同目录 `index.html`。

## 第一层历史对照：真实模型小样本

同一组三道计算任务，无故障注入，宿主检查源数据计算结果。局部修复使用完全相同的首次回答重放；逻辑请求和 token 计入首次回答，后续修复真实请求模型。每策略最多 9 次模型请求。整批重跑在发生失败时追加一轮完整任务。时间不作比较。

| 模型 / 策略 | 通过 | 逻辑请求 | 总 token |
|---|---:|---:|---:|
| MiniCPM5-1B Q4_K_M / 仅验收 | 0/3 | 3 | 1,925 |
| MiniCPM5-1B Q4_K_M / 局部修复 | 0/3 | 9 | 7,331 |
| MiniCPM5-1B Q4_K_M / 整批重跑 | 0/3 | 6 | 3,850 |
| GLM-5.3-Flash / 仅验收 | 3/3 | 3 | 1,836 |
| GLM-5.3-Flash / 局部修复 | 3/3 | 3 | 1,836 |
| GLM-5.3-Flash / 整批重跑（未触发） | 3/3 | 3 | 1,836 |

MiniCPM 的错误均被拦截，但未修好，局部修复反而消耗更多 token。GLM 首次全部正确，说明通过时不额外发起修复请求，不能据此估计修复增益。样本量仅 3，不报告显著性或泛化成功率。两组失败/成功记录完整保留，未筛掉失败样本。

原始记录：交付包 `evidence/live-minicpm/results.json` 与 `evidence/live-glm-flash/results.json`。环境与量化模型 digest 在 `evidence/environment.json`。GLM 使用用户已配置的 Zhipu Coding Plan，证据中不保存密钥。

## 工程验收

- 后端构建与 TypeScript 检查通过；清洁公开副本重新安装依赖后，后端和 UI 生产构建通过。
- agent/tool/gateway/session/config 等 67 个文件的底层回归：300 通过，0 失败，1 跳过。
- 最终清洁公开副本的核心与原生链定向检查：75/75 通过，见 `checks/public-final-tests.txt`。
- 原生网关集成验证：默认继承实际逐轮派发的主模型、单独指定评审模型均通过。
- 最新 UI 消息桥接与评审配置定向检查：61/61 通过；此前更广的配置、路由和界面检查 181 通过，存在重叠，不相加。
- 增加证据缺失拒绝、评审超时、调用用量、共享预算、取消、禁止递归评审、结构化提交边界和声明/文件一致性的回归。
- UI 全量严格类型检查仍受上游 React 18/19 类型声明冲突影响，没有把 UI 生产构建通过写成全量类型检查通过。
- 回放页 DOM 交互检查：5/5，覆盖数据计算、三个策略终态、重置。
- 浏览器可视检查未完成：管理员策略检查不可用导致浏览器工具拒绝打开本地页面。未尝试绕过该限制；不能将 DOM 检查写成可视检查。

日志见 [工程检查](evidence/native-model-review/checks/)。独立审查还修正了系统五字段报告与 JSON 契约冲突、重复历史、无效预算、读取旧答案及拒绝交付被标绿的问题。

## 当前边界

验收质量取决于契约、可信检查器及评审模型；第二层也可能误判。评审必须实际读取声称交付的文件，但“已读取”不能证明理解正确。评审模型默认与主模型相同，独立会话不代表错误完全独立。评审没有写文件权限，拒绝后的修改仍由原生产子任务执行。修复没有回滚外部副作用，不处理共享文件的并行冲突；不实现跨进程恢复。大规模真实任务对照仍未完成。提交项及评审映射依据用户 2026-09-11 补充原文；未提供的官方权重不作推测。
