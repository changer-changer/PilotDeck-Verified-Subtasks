# 验收记忆：真实模型预演与检查证据

日期：2026-09-11。模型：competition/glm-5.3。明确预置语义错误；模型回答、文件读取与修复均实际执行。源文件、最终文件、事件、哈希、用量、原生记忆观察和反馈正文可检查。路径经过脱敏，端点与密钥不公开；移除模型思考增量，保留工具结果、验收判定和用量。

| 运行 | 时长 | 最终交付 | 修复 | 观察记录 | 脚本判定 |
| --- | ---: | ---: | --- | ---: | --- |
| 01-structure-repair | 194.038 秒 | 4/4 | 1 次结构修复，随后提前修好业务 | 4 | 语义拒绝未发生，演示断言失败 |
| 02-full-rehearsal | 215.664 秒 | 4/4 | 1 次结构修复 + 1 次独立语义拒绝后的修复 | 4 | 全部演示断言通过 |

第二轮只调整受控演示指令，将格式修复与业务修复阶段分开；业务标准、评审机制、修复预算均未放宽。两轮三份正确报表的 SHA-256 和修改时间均保持不变。

第二轮四条生产记录合计25模型轮次、161360个totalTokens，包含独立评审用量；不是整个工作流总成本。主任务及原生记忆检索的调用需单独计算，不能把评审小计再次加到子任务用量中。此处没有记忆开关的随机对照，时长不可用于估计记忆的净收益。

01的acceptance-memory.json由同一原生读接口在运行结束后补导出（原脚本在语义断言失败前尚未导出记忆）。02由更新后的演示脚本自动导出。原生Memory反馈是派生文件；ledger字段保存有界原始观察。两次使用独立项目，观察不跨项目混合。

checks/summary.json记录本轮100项后端定向验收、63项UI测试、构建和浏览器限制。16项存储最终复验属于前述测试的重复，不累加。单次/两次闭环不等于总体成功率，且没有实测Dream蒸馏或后续任务准确率提升。
